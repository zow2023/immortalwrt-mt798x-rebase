---
[v2] net: phy: aquantia: add mib debugfs

The users can utilize following command to dump the MIB information
from AQR113C.
  - cat /sys/kernel/debug/aquantia/phyX; X is 0 or 8

This feature is available only on the link partners with speeds of
2.5G, 5G, and 10G.
---
v2: Resolve kernel panic when removing the driver, address overflow issues, and
    implement locking to prevent race conditions between the reader and updater.
v1: Add MIB debugfs support.
---

From 7e5deaa34b7da4b763dc9a458c6ab800b1775c87 Mon Sep 17 00:00:00 2001
From: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
Date: Wed, 6 May 2026 17:30:54 +0800
Subject: [PATCH] net: phy: aquantia: add mib debugfs

The users can utilize following command to dump the MIB information
from AQR113C.
  - cat /sys/kernel/debug/aquantia/phyX; X is 0 or 8

This feature is available only on the link partners with speeds of
2.5G, 5G, and 10G.

Without this patch, it is difficult to locate the source of packet
loss, whether it is on the system side or the line side.

Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
---
 drivers/net/phy/aquantia/Makefile        |   2 +-
 drivers/net/phy/aquantia/aquantia.h      |  17 +++
 drivers/net/phy/aquantia/aquantia_main.c |   4 +
 drivers/net/phy/aquantia/aquantia_mib.c  | 167 +++++++++++++++++++++++
 4 files changed, 189 insertions(+), 1 deletion(-)
 create mode 100755 drivers/net/phy/aquantia/aquantia_mib.c

--- a/drivers/net/phy/aquantia/Makefile
+++ b/drivers/net/phy/aquantia/Makefile
@@ -1,5 +1,5 @@
 # SPDX-License-Identifier: GPL-2.0
-aquantia-objs			+= aquantia_main.o aquantia_firmware.o aquantia_leds.o
+aquantia-objs			+= aquantia_main.o aquantia_mib.o aquantia_firmware.o aquantia_leds.o
 ifdef CONFIG_HWMON
 aquantia-objs			+= aquantia_hwmon.o
 endif
--- a/drivers/net/phy/aquantia/aquantia.h
+++ b/drivers/net/phy/aquantia/aquantia.h
@@ -10,6 +10,7 @@
 #define AQUANTIA_H
 
 #include <linux/device.h>
+#include <linux/mutex.h>
 #include <linux/phy.h>
 
 /* Vendor specific 1, MDIO_MMD_VEND1 */
@@ -174,10 +175,30 @@ static const struct aqr107_hw_stat aqr10
 
 #define AQR107_SGMII_STAT_SZ ARRAY_SIZE(aqr107_hw_stats)
 
+struct aqr107_mib_stat {
+	u64 crc8_error_packets;
+	u64 ldpc_error_packets;
+	u64 ls_tx_good_packets;
+	u64 ls_tx_bad_packets;
+	u64 ls_rx_good_packets;
+	u64 ls_rx_bad_packets;
+	u64 ss_tx_good_packets;
+	u64 ss_tx_bad_packets;
+	u64 ss_rx_good_packets;
+	u64 ss_rx_bad_packets;
+};
+
 struct aqr107_priv {
 	u64 sgmii_stats[AQR107_SGMII_STAT_SZ];
 	unsigned long leds_active_low;
 	unsigned long leds_active_high;
+	struct aqr107_mib_stat mib;
+	struct task_struct *mib_thread;
+	struct dentry *mib_file;
+	/* Serializes mib accumulation by mib_thread against the debugfs
+	 * reader, which snapshots and then clears the counters.
+	 */
+	struct mutex mib_lock;
 };
 
 #if IS_REACHABLE(CONFIG_HWMON)
@@ -186,6 +207,9 @@ int aqr_hwmon_probe(struct phy_device *p
 static inline int aqr_hwmon_probe(struct phy_device *phydev) { return 0; }
 #endif
 
+int aqr_mib_probe(struct phy_device *phydev);
+void aqr_mib_remove(struct phy_device *phydev);
+
 int aqr_firmware_load(struct phy_device *phydev);
 
 int aqr_phy_led_blink_set(struct phy_device *phydev, u8 index,
--- a/drivers/net/phy/aquantia/aquantia_main.c
+++ b/drivers/net/phy/aquantia/aquantia_main.c
@@ -935,9 +935,21 @@ static int aqr107_probe(struct phy_devic
 	if (ret)
 		return ret;
 
-	return aqr_hwmon_probe(phydev);
+	ret = aqr_mib_probe(phydev);
+	if (ret)
+		return ret;
+
+	ret = aqr_hwmon_probe(phydev);
+	if (ret)
+		aqr_mib_remove(phydev);
+
+	return ret;
 }
 
+static void aqr107_remove(struct phy_device *phydev)
+{
+	aqr_mib_remove(phydev);
+}
 
 static struct phy_driver aqr_driver[] = {
 {
@@ -978,6 +990,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR107),
 	.name		= "Aquantia AQR107",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init	= aqr107_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1002,6 +1015,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQCS109),
 	.name		= "Aquantia AQCS109",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init	= aqcs109_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1027,6 +1041,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR111),
 	.name		= "Aquantia AQR111",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init	= aqr107_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1052,6 +1067,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR111B0),
 	.name		= "Aquantia AQR111B0",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init	= aqr107_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1085,6 +1101,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR112),
 	.name		= "Aquantia AQR112",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.config_aneg    = aqr_config_aneg_set_prot,
 	.config_intr	= aqr_config_intr,
 	.handle_interrupt = aqr_handle_interrupt,
@@ -1108,6 +1125,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR412),
 	.name		= "Aquantia AQR412",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.config_aneg    = aqr_config_aneg_set_prot,
 	.config_intr	= aqr_config_intr,
 	.handle_interrupt = aqr_handle_interrupt,
@@ -1126,6 +1144,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR113),
 	.name		= "Aquantia AQR113",
 	.probe          = aqr107_probe,
+	.remove         = aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init    = aqr113c_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1150,6 +1169,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR113C),
 	.name           = "Aquantia AQR113C",
 	.probe          = aqr107_probe,
+	.remove         = aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init    = aqr113c_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1174,6 +1194,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR114C),
 	.name           = "Aquantia AQR114C",
 	.probe          = aqr107_probe,
+	.remove         = aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init    = aqr107_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1199,6 +1220,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR115C),
 	.name           = "Aquantia AQR115C",
 	.probe          = aqr107_probe,
+	.remove         = aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init    = aqr113c_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1224,6 +1246,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR813),
 	.name		= "Aquantia AQR813",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.get_rate_matching = aqr107_get_rate_matching,
 	.config_init	= aqr107_config_init,
 	.config_aneg    = aqr_config_aneg,
@@ -1248,6 +1271,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR112C),
 	.name		= "Aquantia AQR112C",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.config_aneg	= aqr_config_aneg_set_prot,
 	.config_intr	= aqr_config_intr,
 	.handle_interrupt = aqr_handle_interrupt,
@@ -1260,6 +1284,7 @@ static struct phy_driver aqr_driver[] =
 	PHY_ID_MATCH_MODEL(PHY_ID_AQR112R),
 	.name		= "Aquantia AQR112R",
 	.probe		= aqr107_probe,
+	.remove		= aqr107_remove,
 	.config_aneg	= aqr_config_aneg_set_prot,
 	.config_intr	= aqr_config_intr,
 	.handle_interrupt = aqr_handle_interrupt,
--- /dev/null
+++ b/drivers/net/phy/aquantia/aquantia_mib.c
@@ -0,0 +1,217 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Packet counter driver for Aquantia PHY
+ */
+
+#include <linux/phy.h>
+#include <linux/kernel.h>
+#include <linux/debugfs.h>
+#include <linux/kthread.h>
+
+#include "aquantia.h"
+
+#define MDIO_PCS_LS_TX_GOOD_COUNTER		0xc820
+#define MDIO_PCS_LS_TX_BAD_COUNTER		0xc822
+#define MDIO_PCS_SS_TX_GOOD_COUNTER		0xc860
+#define MDIO_PCS_SS_TX_BAD_COUNTER		0xc862
+#define MDIO_PCS_CRC8_ERROR_COUNTER		0xe810
+#define MDIO_PCS_LS_RX_GOOD_COUNTER		0xe812
+#define MDIO_PCS_LS_RX_BAD_COUNTER		0xe814
+#define MDIO_PCS_LDPC_ERROR_COUNTER		0xe820
+#define MDIO_PCS_SS_RX_GOOD_COUNTER		0xe860
+#define MDIO_PCS_SS_RX_BAD_COUNTER		0xe862
+
+static int aqr107_mib_read_word(struct phy_device *phydev, u32 reg, u16 *lsw, u16 *msw)
+{
+	int val;
+
+	val = phy_read_mmd(phydev, MDIO_MMD_PCS, reg + 1);
+	if (val < 0)
+		return val;
+
+	*msw = val;
+
+	val = phy_read_mmd(phydev, MDIO_MMD_PCS, reg);
+	if (val < 0)
+		return val;
+
+	*lsw = val;
+
+	return 0;
+}
+
+static void aqr107_mib_read(struct phy_device *phydev)
+{
+	struct aqr107_priv *priv = phydev->priv;
+	u16 lsw, msw;
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_CRC8_ERROR_COUNTER, &lsw, &msw))
+		priv->mib.crc8_error_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_LDPC_ERROR_COUNTER, &lsw, &msw))
+		priv->mib.ldpc_error_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_LS_TX_GOOD_COUNTER, &lsw, &msw))
+		priv->mib.ls_tx_good_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_LS_TX_BAD_COUNTER, &lsw, &msw))
+		priv->mib.ls_tx_bad_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_LS_RX_GOOD_COUNTER, &lsw, &msw))
+		priv->mib.ls_rx_good_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_LS_RX_BAD_COUNTER, &lsw, &msw))
+		priv->mib.ls_rx_bad_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_SS_TX_GOOD_COUNTER, &lsw, &msw))
+		priv->mib.ss_tx_good_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_SS_TX_BAD_COUNTER, &lsw, &msw))
+		priv->mib.ss_tx_bad_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_SS_RX_GOOD_COUNTER, &lsw, &msw))
+		priv->mib.ss_rx_good_packets += (((u32)msw << 16) | lsw);
+
+	if (!aqr107_mib_read_word(phydev, MDIO_PCS_SS_RX_BAD_COUNTER, &lsw, &msw))
+		priv->mib.ss_rx_bad_packets += (((u32)msw << 16) | lsw);
+}
+
+static int aqr107_mib_thread(void *data)
+{
+	struct phy_device *phydev = data;
+	struct aqr107_priv *priv = phydev->priv;
+
+	for (;;) {
+		if (kthread_should_stop())
+			break;
+
+		mutex_lock(&priv->mib_lock);
+		aqr107_mib_read(phydev);
+		mutex_unlock(&priv->mib_lock);
+
+		set_current_state(TASK_INTERRUPTIBLE);
+		schedule_timeout(HZ);
+	}
+
+	return 0;
+}
+
+static int aqr107_mib_show(struct seq_file *m, void *private)
+{
+	struct phy_device *phydev = m->private;
+	struct aqr107_priv *priv = phydev->priv;
+	struct aqr107_mib_stat mib;
+
+	mutex_lock(&priv->mib_lock);
+	aqr107_mib_read(phydev);
+	mib = priv->mib;
+	memset(&priv->mib, 0, sizeof(priv->mib));
+	mutex_unlock(&priv->mib_lock);
+
+	seq_printf(m, "+---------------------------------+\n");
+	seq_printf(m, "|         <<AQUANTIA MIB>>        |\n");
+	seq_printf(m, "| CRC8 Error Packets=%012llu |\n", mib.crc8_error_packets);
+	seq_printf(m, "| LDPC Error Packets=%012llu |\n", mib.ldpc_error_packets);
+	seq_printf(m, "|           [Line Side]\n");
+	seq_printf(m, "| TX   Good  Packets=%012llu |\n", mib.ls_tx_good_packets);
+	seq_printf(m, "| TX   Bad   Packets=%012llu |\n", mib.ls_tx_bad_packets);
+	seq_printf(m, "| RX   Good  Packets=%012llu |\n", mib.ls_rx_good_packets);
+	seq_printf(m, "| RX   Bad   Packets=%012llu |\n", mib.ls_rx_bad_packets);
+	seq_printf(m, "|          [System Side]\n");
+	seq_printf(m, "| TX   Good  Packets=%012llu |\n", mib.ss_tx_good_packets);
+	seq_printf(m, "| TX   Bad   Packets=%012llu |\n", mib.ss_tx_bad_packets);
+	seq_printf(m, "| RX   Good  Packets=%012llu |\n", mib.ss_rx_good_packets);
+	seq_printf(m, "| RX   Bad   Packets=%012llu |\n", mib.ss_rx_bad_packets);
+	seq_printf(m, "+---------------------------------+\n");
+
+	return 0;
+}
+
+static int aqr107_mib_open(struct inode *inode, struct file *file)
+{
+	return single_open(file, aqr107_mib_show, inode->i_private);
+}
+
+static const struct file_operations fops_mib = {
+	.owner = THIS_MODULE,
+	.open = aqr107_mib_open,
+	.read = seq_read,
+	.llseek = seq_lseek,
+	.release = single_release
+};
+
+int aqr_mib_probe(struct phy_device *phydev)
+{
+	struct aqr107_priv *priv = phydev->priv;
+	struct dentry *root;
+	char dirname[8];
+	bool dput_needed = false;
+	int ret = 0;
+
+	mutex_init(&priv->mib_lock);
+
+	snprintf(dirname, sizeof(dirname), "phy%d", phydev->mdio.addr);
+
+	root = debugfs_lookup("aquantia", NULL);
+	if (!IS_ERR_OR_NULL(root)) {
+		/* a dentry from lookup() needs dput() after we don't use it */
+		dput_needed = true;
+	} else {
+		root = debugfs_create_dir("aquantia", NULL);
+		if (IS_ERR(root))
+			return PTR_ERR(root);
+	}
+
+	priv->mib_file = debugfs_create_file(dirname, 0444, root, phydev,
+					     &fops_mib);
+	if (IS_ERR(priv->mib_file)) {
+		ret = PTR_ERR(priv->mib_file);
+		phydev_err(phydev, "failed to create %s debugfs file (%d)\n",
+			   dirname, ret);
+		priv->mib_file = NULL;
+		goto out;
+	}
+
+	/* create a thread for recording packet counts */
+	priv->mib_thread = kthread_create(aqr107_mib_thread,
+					  phydev,
+					  "aqr107_mib_thread");
+	if (IS_ERR(priv->mib_thread)) {
+		ret = PTR_ERR(priv->mib_thread);
+		phydev_err(phydev,
+			   "failed to create aqr107_mib_thread (%d)\n",
+			   ret);
+		priv->mib_thread = NULL;
+		debugfs_remove(priv->mib_file);
+		priv->mib_file = NULL;
+		goto out;
+	}
+	wake_up_process(priv->mib_thread);
+
+out:
+	if (dput_needed)
+		dput(root);
+
+	return ret;
+}
+
+void aqr_mib_remove(struct phy_device *phydev)
+{
+	struct aqr107_priv *priv = phydev->priv;
+
+	if (!priv)
+		return;
+
+	/* Remove the debugfs file first: debugfs_remove() waits for readers
+	 * already inside aqr107_mib_show() to leave, so nothing can touch
+	 * priv->mib or priv->mib_lock afterwards.
+	 */
+	debugfs_remove(priv->mib_file);
+	priv->mib_file = NULL;
+
+	if (priv->mib_thread) {
+		kthread_stop(priv->mib_thread);
+		priv->mib_thread = NULL;
+	}
+
+	mutex_destroy(&priv->mib_lock);
+}
